<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="./public/css/styles.css">
    <title>Planner</title>
  </head>
  <body>
    <header>
      <h1>Chore Planner</h1>
      <nav>
        <a href="/plans/new"><button>Add a Plan!</button></a>
      </nav>
    </header>
    <div class="container">
      <% for (let i=0; i < plan.length; i++) { %>
        <a class="plan-link" href="/plans/<%=plan[i]._id%>">
          <div class="plan-container">
            <h2><%=plan[i].name%></h4>
            <h3><%=plan[i].importance%></h3>
            <h3><%=plan[i].progress%></h3>
            <h3><%=plan[i].deadline%></h3>
          </div>
        </a>
      <% } %>
    </div>
  </body>
</html>
